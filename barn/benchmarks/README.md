# Barn Benchmark Records

Benchmark JSON under `results/` is evidence, not marketing copy. Existing records are immutable; reruns should write a new dated/identified file.

## Evidence grades

- `synthetic_smoke_not_model_performance`: deterministic worker used to validate coordination, worktree, accounting, acceptance, and replay machinery.
- future provider-backed grades must name the provider/model/runtime version and matched budget contract.

## 2026-09-12 synthetic comparison

`results/2026-09-12-synthetic-comparison.json` compares a strong single-agent policy, a fixed specialist team, and Barn under the same three-turn ceiling. All pass the tiny acceptance contract. The single policy uses one turn; fixed-team and Barn use two. Barn staffs two agents rather than fixed-team's three.

This is a negative/simple-task baseline for Barn's efficiency and should remain so.
